#include"main.h"
/**
*_abs - prints absolute value of number n
*@n: input number
*Return: returns the absolute value of n
**/
int _abs(int n)
{
if (n < 0)
{
	n = n * -1;
	return (n);
}
else
{
	return (n);
}
}
