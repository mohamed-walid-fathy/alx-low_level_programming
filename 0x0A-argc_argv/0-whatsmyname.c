#include <stdio.h>

/**
 * main - entry point
 * Description: prints program name
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
    argc = 0;
    printf("%s \n", argv[argc]);
    return (0);
}