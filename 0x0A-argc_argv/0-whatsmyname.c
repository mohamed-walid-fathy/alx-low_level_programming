#include <stdio.h>

/**
*main - entry point
*argc: int of arguments
*argv: array of arguments
*Return: Always 0
*/

int main(int __attribute__((unused)) argc, char const *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
